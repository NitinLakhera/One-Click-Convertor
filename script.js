function launchTool(toolName) {
  alert(toolName + ' tool will launch soon!');
  // Replace the alert with actual navigation or functionality
  // Example: window.location.href = 'tools/pdf-to-image.html';
}